 <%= render "components/index_navbar" %>
<div class="background-style">
  <div class="create-container">
    <div class="row justify-content-center">
      <div class="col-8 text-align align-items-center">
        <h1 style="text-align: center; text-decoration: underline;">Create List</h1>
        <%= simple_form_for [@list] do |f| %>
          <%= f.input :name, label: "List Name" %>
          <%= f.submit 'Create', class: 'btn btn-secondary mt-2' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
