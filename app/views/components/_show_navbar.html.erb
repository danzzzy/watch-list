<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto" id="navbarSupportedContent">
      <li class="nav-item">
        <%= link_to "Home", lists_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Add Movie", new_list_bookmark_path(@list), class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Create List", new_list_path, class: "nav-link current_class?(new_list_path)" %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" id= "navbarDropdown" role="button" data-toggle= "dropdown" aria-haspopup= "true" aria-expanded= "false">
         My Lists
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <% @lists.each do |list| %>
        <div class="dropdown-category dropdown-item" >
        <%= link_to list.name, list_path(list), class: "dropdown-text" %>
        </div>
         <% end %>
         <%# <li class="nav-item">
          <%= link_to "Back", request.referer.present? ? request.referer : default_path, class: "nav-link" %>
        <%# </li> %>
    </ul>
  </div>
</div>
